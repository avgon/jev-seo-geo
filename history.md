## 2026-09-20T00:48:46+03:00 | Measurement integrity hardening

- Request: Apply approved audit fixes to the existing library only, on a new local branch, with offline tests and no pushes, releases, secrets or live/paid API calls.
- Changes: Added the implementation plan and shared strict Jev validation; normalized every raw structure score by two; replaced independent brand generations with shared query/provider/sample observations, Unicode-boundary mention detection and evidenced numbered-item ordinals; added sanitized errors, nullable unknown measurements, success/error counts, timestamps/model/method metadata and audit failure states. Removed silent input truncation in favor of a 12,000-character limit. Hardened provider configuration/contracts, preserved multi-part text and rejected explicitly truncated provider outputs. Fixed focused checklist deduplication and missing-author/source actions. Added explicit unverified rewrite statuses, retained drafts on scoring failure, VERIFY placeholders and human-review metadata. Rewrote English README/FAQ and Turkish usage documentation with runnable APIs and exact limits. Version remains 0.1.1 because this is a local hardening commit, not a release.
- Risks/limits: Nullable measurements, strict response schemas, tie ranks, explicit-key semantics and query_all status dictionaries are intentional compatibility changes. Literal matching does not resolve aliases or segment unspaced languages. Provider model availability and actual SEO uplift are unverified. No crawl, Perplexity integration, factual verification or consumer ChatGPT ranking measurement. Python 3.9+ is intended; only Python 3.13.5 was runtime-tested, with a Python 3.9 AST syntax check. Accepted input coverage is bounded; generated output completeness is not guaranteed.
- Tests: `python -m unittest discover -s tests -v` passed all 20 tests (0.245s), including retained workflow smoke tests, shared generation counts, failures/partial denominators, boundary/rank evidence, strict missing/invalid/NaN fields, limits, focused checklist actions, rewrite failure retention, provider HTTP/malformed/empty/multipart/truncation contracts, HTML escaping and executable README examples. `git diff --check` passed. `python -m build --wheel` was unavailable (build module absent); fallback `python -m pip wheel . --no-deps --wheel-dir dist` succeeded with isolated build dependencies. Final wheel SHA256: 494b1da7d8885ba8eb6370cac70a57678cf45743c7b345c8e979ae2a6ead5304. A fresh temporary virtualenv installed that wheel using `--no-index --no-deps`; `python -I` imported all public modules from site-packages and passed offline probe/no-provider audit checks. No live provider requests were made; only build dependencies could access the package index.
- Rollback: Revert this local implementation commit on `fix/measurement-integrity`; its parent is `ae890e43fb36845b15a05d9656d9c4b890d0eab7`. No remote changes, releases or external state require rollback.
