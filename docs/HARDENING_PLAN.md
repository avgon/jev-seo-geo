# Measurement integrity hardening

Scope: existing Python library only. No live/paid API requests, publishing or pushes.

1. Validate Jev schemas and finite numeric ranges with one shared validator; reject oversized input instead of truncating.
2. Capture each provider/query/sample once, record sanitized failures and metadata, and derive all brand comparisons from shared successful observations. Use literal Unicode boundaries and only evidenced numbered-list ordinals.
3. Make audits and rewrites explicitly report partial/failure states; preserve drafts on scoring failure, require human verification, and fix focused checklist deduplication.
4. Replace misleading documentation with runnable examples, limits, credential/privacy/cost boundaries and intentional API changes.
5. Add offline contract/regression tests, run unittest and isolated wheel/import checks, record actual results in one history entry, and commit only scoped changes locally.
